<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E9DFBF"
    android:gravity="center"
    android:orientation="vertical">

    <!-- Picture of Food -->

    <!-- GIF of Dish Dash Logo -->

    <!-- Button to Continue -->

    <ImageView
        android:id="@+id/dish_dash_logo"
        android:layout_width="0dp"
        android:layout_height="395dp"
        android:layout_marginBottom="30dp"
        android:contentDescription="Dish Dash Logo GIF"
        android:src="@drawable/dish_dash_logo"
        app:layout_constraintBottom_toTopOf="@+id/continue_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageView
        android:id="@+id/food_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Picture of food"
        android:scaleType="centerCrop"
        android:src="@drawable/food_image"
        app:layout_constraintStart_toStartOf="@+id/dish_dash_logo"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/continue_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="35dp"
        android:text="Click here to continue"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
